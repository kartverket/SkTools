apply plugin: 'maven'
apply plugin: 'java'
apply plugin: 'groovy'

dependencies {

    //Saxon for xslt 2.0
    //Saxon HE is the open source version
    runtime 'net.sf.saxon:Saxon-HE:9.5.1-8'

    //testing...
    testCompile gradleApi()
    testCompile project(':build-utils:test-utils')

}

task sourcesJar(type: Jar, dependsOn:classes) {
    classifier = 'sources'
    from sourceSets.main.allSource
    onlyIf { gradle.taskGraph.hasTask(install) || gradle.taskGraph.hasTask(uploadArchives) } //only when published..
}


artifacts {
    archives sourcesJar
}
